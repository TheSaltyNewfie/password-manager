#include <iostream>

/*
void draw_menu(int version_number){
    ImGui::Text("Admin Menu %d", version_number);

    if(ImGui::Button("Start"))
        std::cout << "This would start the server, but its not ready";
    if(ImGui::Button("Halt"))
        std::cout << "This would halt the server, but its not ready";

}*/